name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: mobile_app/package-lock.json

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install npm dependencies
        run: |
          cd mobile_app
          npm install

      - name: Expo prebuild (génère le projet Android natif)
        run: |
          cd mobile_app
          npx expo prebuild --platform android --no-install

      - name: Bundle le JS dans les assets Android
        run: |
          cd mobile_app
          mkdir -p android/app/src/main/assets
          npx react-native bundle \
            --platform android \
            --dev false \
            --entry-file index.ts \
            --bundle-output android/app/src/main/assets/index.android.bundle \
            --assets-dest android/app/src/main/res

      - name: Build APK
        run: |
          cd mobile_app/android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK comme artefact
        uses: actions/upload-artifact@v4
        with:
          name: IoT-Sentinelle-standalone
          path: mobile_app/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 90

      - name: Créer ou mettre à jour la Release GitHub
        uses: softprops/action-gh-release@v2
        with:
          tag_name: apk-latest
          name: "IoT-Sentinelle — APK Android (dernière version)"
          body: |
            APK autonome généré automatiquement depuis la branche `main`.
            Le bundle JavaScript est inclus — **aucun Metro ni PC requis**.

            **Installation :**
            1. Transférer `app-debug.apk` sur votre Android
            2. Activer "Sources inconnues" dans les paramètres
            3. Ouvrir le fichier APK pour installer

            **Permissions requises :** Bluetooth, Caméra, Localisation
          files: mobile_app/android/app/build/outputs/apk/debug/app-debug.apk
          make_latest: true
